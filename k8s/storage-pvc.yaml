apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: wavevizual-storage
  namespace: wavevizual
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
  storageClassName: local-path
